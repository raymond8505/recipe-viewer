{
  "mcpServers": {
    "recipe-viewer-storybook": {
      "url": "http://${SB_HOST:-localhost}:${SB_PORT:-6006}/mcp",
      "type": "http"
    },
    "shadcn": {
      "command": "npx",
      "args": [
        "shadcn@latest",
        "mcp"
      ]
    }
  }
}
